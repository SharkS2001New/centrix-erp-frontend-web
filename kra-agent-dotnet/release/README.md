# Shop-facing Centrix KRA Agent binaries (no source).
#
# Build / refresh locally (macOS, Linux, or Windows with .NET 8 SDK):
#   ./scripts/stage-release.sh
#   # or on Windows: .\scripts\stage-release.ps1
#
# Finance → Download serves release/win-x64 + injected config.json.
# Deploy this folder with the Centrix web host, or set KRA_AGENT_RELEASE_DIR.
