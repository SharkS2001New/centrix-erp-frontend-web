# Shop-facing Centrix KRA Agent binaries (no source).
#
# On a Windows build PC with .NET 8 SDK:
#   .\scripts\stage-release.ps1
#
# Deploy the resulting win-x64\ folder here (or set KRA_AGENT_RELEASE_DIR / KRA_AGENT_RELEASE_URL
# on the Centrix web host). Finance → Download serves this folder + injected config.json.
